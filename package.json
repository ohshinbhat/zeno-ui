{
  "name": "zeno-ui",
  "private": true,
  "version": "0.1.0",
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "yarn workspace @zeno-ui/react typecheck && yarn workspace @zeno-ui/react-native typecheck",
    "build:packages": "yarn workspace @zeno-ui/react build && yarn workspace @zeno-ui/react-native build",
    "pack:packages": "node ./scripts/pack-packages.mjs",
    "release:check": "yarn typecheck && yarn build:packages && yarn pack:packages"
  },
  "devDependencies": {
    "@types/react": "^18.3.4",
    "react": "^18.3.1",
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  }
}

